server {
    listen 80;
    server_name {{ nginx_app_server_name }};

    root {{ nginx_app_root }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /healthz {
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
